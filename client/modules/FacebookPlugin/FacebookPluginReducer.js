// Import Actions
import {  } from './FacebookPluginActions';

// Initial State
const initialState = {};

const FacebookPluginReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default FacebookPluginReducer;
